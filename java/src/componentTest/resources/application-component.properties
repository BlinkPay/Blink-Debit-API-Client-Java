# Component test configuration
# Uses WireMock on localhost:8888

# WireMock is configured on port 8888
blinkpay.debit.url=http://localhost:8888

# Test credentials for WireMock
blinkpay.client.id=test-client-id
blinkpay.client.secret=test-client-secret

# Retry configuration
blinkpay.retry.enabled=false